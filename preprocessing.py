import tensorflow as tf
import shutil
import os

# Used for optimizing datasets
AUTOTUNE = tf.data.AUTOTUNE


def load_dataset():
    """
      Loads the training and testing datasets
    """
    pass